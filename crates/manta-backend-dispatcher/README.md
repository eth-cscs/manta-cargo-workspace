This crate defines the manta public interface:
- functions
- types

Functions with name starting with 'i_' means the function is 'interactive' therefore look if this function accepts `--assume-yes` to run in unatended mode
